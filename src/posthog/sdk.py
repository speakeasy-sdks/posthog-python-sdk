
import requests
from . import utils
from .actions import Actions
from .activity_log import ActivityLog
from .annotations import Annotations
from .app_metrics import AppMetrics
from .cohorts import Cohorts
from .dashboard_templates import DashboardTemplates
from .dashboards import Dashboards
from .domains import Domains
from .event_definitions import EventDefinitions
from .events import Events
from .experiments import Experiments
from .exports import Exports
from .feature_flags import FeatureFlags
from .funnel import Funnel
from .groups import Groups
from .groups_types import GroupsTypes
from .hooks import Hooks
from .ingestion_warnings import IngestionWarnings
from .insights import Insights
from .integrations import Integrations
from .invites import Invites
from .is_generating_demo_data import IsGeneratingDemoData
from .members import Members
from .organizations import Organizations
from .performance_events import PerformanceEvents
from .persons import Persons
from .plugin_configs import PluginConfigs
from .plugins import Plugins
from .projects import Projects
from .prompts import Prompts
from .property_definitions import PropertyDefinitions
from .query import Query
from .reset_token import ResetToken
from .resource_access import ResourceAccess
from .roles import Roles
from .session_recording_playlists import SessionRecordingPlaylists
from .session_recordings import SessionRecordings
from .subscriptions import Subscriptions
from .tags import Tags
from .trend import Trend
from .uploaded_media import UploadedMedia

SERVERS = [
	"https://app.posthog.com/api/",
]


class PostHog:
    
    actions: Actions
    activity_log: ActivityLog
    annotations: Annotations
    app_metrics: AppMetrics
    cohorts: Cohorts
    dashboard_templates: DashboardTemplates
    dashboards: Dashboards
    domains: Domains
    event_definitions: EventDefinitions
    events: Events
    experiments: Experiments
    exports: Exports
    feature_flags: FeatureFlags
    funnel: Funnel
    groups: Groups
    groups_types: GroupsTypes
    hooks: Hooks
    ingestion_warnings: IngestionWarnings
    insights: Insights
    integrations: Integrations
    invites: Invites
    is_generating_demo_data: IsGeneratingDemoData
    members: Members
    organizations: Organizations
    performance_events: PerformanceEvents
    persons: Persons
    plugin_configs: PluginConfigs
    plugins: Plugins
    projects: Projects
    prompts: Prompts
    property_definitions: PropertyDefinitions
    query: Query
    reset_token: ResetToken
    resource_access: ResourceAccess
    roles: Roles
    session_recording_playlists: SessionRecordingPlaylists
    session_recordings: SessionRecordings
    subscriptions: Subscriptions
    tags: Tags
    trend: Trend
    uploaded_media: UploadedMedia

    _client: requests.Session
    _security_client: requests.Session
    
    _server_url: str = SERVERS[0]
    _language: str = "python"
    _sdk_version: str = "0.1.1"
    _gen_version: str = "1.3.2"

    def __init__(self) -> None:
        self._client = requests.Session()
        self._security_client = requests.Session()
        self._init_sdks()


    def config_server_url(self, server_url: str, params: dict[str, str]):
        if params is not None:
            self._server_url = utils.replace_parameters(server_url, params)
        else:
            self._server_url = server_url

        self._init_sdks()
    

    def config_client(self, client: requests.Session):
        self._client = client
        self._init_sdks()
    
    
    def _init_sdks(self):
        
        self.actions = Actions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.activity_log = ActivityLog(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.annotations = Annotations(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.app_metrics = AppMetrics(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.cohorts = Cohorts(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.dashboard_templates = DashboardTemplates(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.dashboards = Dashboards(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.domains = Domains(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.event_definitions = EventDefinitions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.events = Events(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.experiments = Experiments(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.exports = Exports(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.feature_flags = FeatureFlags(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.funnel = Funnel(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.groups = Groups(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.groups_types = GroupsTypes(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.hooks = Hooks(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.ingestion_warnings = IngestionWarnings(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.insights = Insights(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.integrations = Integrations(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.invites = Invites(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.is_generating_demo_data = IsGeneratingDemoData(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.members = Members(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.organizations = Organizations(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.performance_events = PerformanceEvents(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.persons = Persons(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.plugin_configs = PluginConfigs(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.plugins = Plugins(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.projects = Projects(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.prompts = Prompts(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.property_definitions = PropertyDefinitions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.query = Query(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.reset_token = ResetToken(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.resource_access = ResourceAccess(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.roles = Roles(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.session_recording_playlists = SessionRecordingPlaylists(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.session_recordings = SessionRecordings(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.subscriptions = Subscriptions(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.tags = Tags(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.trend = Trend(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
        
        self.uploaded_media = UploadedMedia(
            self._client,
            self._security_client,
            self._server_url,
            self._language,
            self._sdk_version,
            self._gen_version
        )
    
    